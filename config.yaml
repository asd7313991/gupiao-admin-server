# Configuration file for the admin server
server:
  port: 8080
  max_body_size: "10MB"
  max_header_bytes: 1048576
  shutdown_timeout: "10s"
  read_timeout: "30s"
  write_timeout: "30s"
  idle_timeout: "120s"
  enable_rate_limit: false
  global_rate_limit: 100
  global_rate_burst: 200

jwt:
  key: "8hDe2TnbG3fQkiD82D2gqqlNVtusZl7q"  # Change this key before deployment
  expiration: "12h"  # JWT token expiration time

log:
  max_size: 50
  max_backups: 3
  max_age: 30

redis:
  host: "127.0.0.1:6379"  # Redis server address
  password: "izpXvn894uW2HFbyP5OGr"  # Redis password

postgres:
  host: "127.0.0.1"
  user: "postgres"
  password: "kL81xnDWo221FHFRX8GnP"  # PostgreSQL password
  dbname: "server"
  port: 5432
  sslmode: "disable"
  timezone: "Asia/Shanghai"

admin:
  password: "123456"  # Admin password - MUST be changed in production
  salt: "rHECMvW3el1zhpdzgx9dY"  # Password salt - MUST be regenerated and kept secret

security:
  google_auth_enabled: false
  google_auth_secret: ""

rate_limit:
  login_rate_per_minute: 30    # Maximum login attempts per minute per IP
  login_burst_size: 60         # Burst size for login requests
  general_rate_per_sec: 100    # Maximum general API requests per second per IP
  general_burst_size: 200      # Burst size for general API requests

tenant:
  min_query_length: 3          # Minimum length of input for tenant code search (must be met or exceeded)
  default_code: "platform"       # System default tenant code
